// API Configuration
// Change this based on your setup:
// - For Expo Go on physical device: Use your computer's IP address
// - For iOS Simulator: Can use localhost or IP address  
// - For Android Emulator: Use 10.0.2.2 or your computer's IP address
// - For web: Use localhost

const CONFIG = {
  // Your computer's IP address (found via ifconfig)
  API_BASE_URL: 'http://192.168.10.61:3001/api',
  
  // Alternative configurations you can use:
  // LOCALHOST: 'http://localhost:3001/api',           // For web or iOS simulator
  // ANDROID_EMULATOR: 'http://10.0.2.2:3001/api',    // For Android emulator
};

export default CONFIG;
